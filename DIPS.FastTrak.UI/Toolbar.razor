<div class="Toolbar @CssClass">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [CascadingParameter]
    public Panel? Panel { get; set; }

    private string CssClass => Panel != null ? "Separator" : "";
}